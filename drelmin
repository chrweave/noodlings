#!/bin/bash

cd ~/noodlings
git pull
cd ~/caw
find ~/linux -type f -print0| grep -z -v '/\.git/' |xargs -0 -n1 bash gut.sh >~/noodlings/drelmout
cd ~/noodlings
git commit -m test$(dk) drelmout
git push
