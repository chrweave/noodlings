#!/bin/bash
cd /home/cweaver/noodlings

# manoon
git fetch origin master
git reset --hard origin/master
echo "Why are we here? because we"\'"re here." >drelmout
#bash ../caw/wurdfold
echo drok$(date +%Y%m%d%H%M) > ../caw/blep
git stage drelmout
git commit -F ../caw/blep drelmout
git push
